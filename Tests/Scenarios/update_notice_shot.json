{
  "name": "update_notice_shot",
  "saveFile": "minimal_colony.rws",
  "description": "Photographs the update notice. NEEDS --keep-dialogs, or the guard refuses the window at Add and the screenshot is a plain colony -- the runner names the dropped window when that happens. Raises it through RaiseWindow rather than letting the real UIRoot_Entry.Init postfix do it, because a window raised at the main menu is discarded when UIRoot_Play takes over and no flag can change that; Dialog_UpdateNotice's parameterless constructor reads the same live settings the real path does, so the frame shows the real thing.\n\nWHICH STATE IS SHOT IS DECIDED BY THE SETTINGS FILE, not by this scenario, since the notice reads persisted state: <vectorLights>False</vectorLights> plus no <updateNoticeVersion> gives the upgrade case (offer + cloud announcement), and deleting the whole settings file gives the first-install case (offer only, different title). Waits in FRAMES, not ticks -- a forcePause modal stops the clock, so a tick-based wait would hang here.",
  "steps": [
    { "type": "SetTime", "args": { "hour": "12" } },
    { "type": "RaiseWindow", "args": { "type": "CelestialLighting.Dialog_UpdateNotice" } },
    { "type": "Wait", "args": { "frames": "5" } },
    { "type": "Screenshot", "args": { "fileName": "update_notice_shot.png" } }
  ]
}
